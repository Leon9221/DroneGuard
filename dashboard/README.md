Dashboard assets are served from `templates/dashboard.html` and `static/`.

This folder is reserved for future modular dashboard components such as chart configs,
sector maps, and reusable operator console widgets.
